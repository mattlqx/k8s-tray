# k8s-tray Copilot Instructions

Builds should be done with `make build-all`.

Build and deployment for macOS should be done with `make deploy-macos HOST=mac-mini-m4.local`.

ADRs are located in the `adrs` directory.
